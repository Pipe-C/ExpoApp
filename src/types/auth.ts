export interface UserCredentials {
  username: string;
  password: string;
}

export interface UserProfileData {
  username: string;
  fullName: string;
  email: string;
  role: string;
  city: string;
  avatarUrl: string;
}